
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access for authenticated users to all collections
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

    // Example of more specific rules you might use later:
    // match /categories/{categoryId} {
    //   allow read: if true; // Allow public read or if request.auth != null;
    //   allow create, update, delete: if request.auth != null; // Only authenticated users can write
    // }
    // match /products/{productId} {
    //   allow read: if true;
    //   allow create, update, delete: if request.auth != null;
    // }
    // match /customers/{customerId} {
    //   allow read, create, update, delete: if request.auth != null;
    // }
    // match /orders/{orderId} {
    //  allow read, create: if request.auth != null;
      // allow update, delete: if request.auth != null && resource.data.userId == request.auth.uid; // Example: only owner can modify
    // }
  }
}
